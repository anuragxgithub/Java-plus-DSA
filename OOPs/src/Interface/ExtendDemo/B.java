package Interface.ExtendDemo;
//As we know we can extend/inherit interfaces as well. But we must have to provide the implementations of both
//the interfaces from the class which implements them.
public interface B extends A{
    void greet();
}
